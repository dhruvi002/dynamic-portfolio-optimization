# Python
__pycache__/
*.py[cod]
*.egg-info/
.env

# Data & cache (too large for GitHub)
data/raw_data.parquet
data/processed_data.parquet
data/nasdaq_exteral_data.csv
data/sentiment_scores.parquet
modularization-demo/

# Ray Tune results
ray_results/

# Run artifacts (regenerated each run — don't commit the large binaries).
# Commit source + the small JSON/CSV/PNG summaries explicitly if you want them.
checkpoints/
runs/
plots/
experiments/results/checkpoints/
experiments/results/*.npz
*.pt
*.pkl

# Conda
.conda/

# macOS
.DS_Store

# Jupyter
.ipynb_checkpoints/
